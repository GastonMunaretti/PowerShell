###################################################################################################################
###################################################################################################################
#####################################    Consulta de todos los grupos   ###########################################

dsquery group -limit 1000 #gropos del directorio


###################################################################################################################
###################################################################################################################
##########################   Consultas varias #####################################################################

####   dsquery user -name gaston* | dsget user -samid -display

####   dsquery group dc=intercargo,dc=com,dc=ar -limit 1000 #muestra todos los grupos






###################################################################################################################
###################################################################################################################

$list = dsquery group -samid "Navegacion Full" | dsget group -members -expand 

foreach ($servers in $list)
{
if  ($servers.indexof("CN") -ge  0 ){
    $servers.Split(',')[0].SubString(4)
    }
}

###################################################################################################################
###################################################################################################################
###############################  Consulta usuarios Navegacion Full  ###############################################

$list = dsquery group -samid "Navegacion Full" | dsget group -members -expand 

$rr=foreach ($servers in $list)
{
if  ($servers -match "CN"){
    $servers.Split(',')[0].SubString(4)
    }
}

###################################################################################################################
###################################################################################################################
####################################  Consulta usuarios CursoGDE  #################################################

$listGDE = dsquery group -samid "CursoGDE" | dsget group -members -expand 

foreach ($servers in $listGDE)
{
if  ($servers -match "CN"){
    $servers.Split(',')[0].SubString(4)
    }
}

##################################################################################################################
##################################################################################################################

$listGDE = dsquery group -samid "Navegacion Restringido" | dsget group -members -expand 

foreach ($servers in $listGDE)
{
if  ($servers -match "CN"){
    $servers.Split(',')[0].SubString(4)
    }
}
